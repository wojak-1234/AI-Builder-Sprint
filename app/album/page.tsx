import AlbumClient from "./AlbumClient";

export const metadata = {
  title: "이음 - 빛바랜 추억 사진첩",
  description: "옛 사진과 앨범 속에 담긴 소중한 이야기와 답변을 한눈에 모아보는 사진첩",
};

export default function AlbumPage() {
  return <AlbumClient />;
}
